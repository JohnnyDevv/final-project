<% if obj.errors.any? %>
  <div class="alert alert-danger alert-dismissible fade show" role="alert">
    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
      <span aria-hidden="true">&times;</span>
    </button>
    <h3><%= pluralize(obj.errors.count, "error") %> prohibited this <%= controller_name.classify.constantize %> from being saved:</h3>
    <ul>
      <% obj.errors.full_messages.each do |msg| %>
        <!--<li><%#= msg %></li>-->
        <h3>Revoked!</h3>
        <li>Gateway Rejected: <strong>fraud</strong></li>
      <% end %>
    </ul>
  </div>
<% end %>  


